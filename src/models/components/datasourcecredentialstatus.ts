/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { ClosedEnum } from "../../types/enums.js";

/**
 * Lifecycle state of the credentials installed for a datasource instance. Mirrors the internal admin Status enum so the handler can surface the same health signals already tracked today. EXPIRING_SOON is represented as VALID_WITH_WARNINGS (with detail in `message`); EXPIRED is surfaced as INVALID plus a non-null `expiresAt` in the past.
 *
 * @remarks
 */
export const DatasourceCredentialStatus = {
  Valid: "VALID",
  ValidWithWarnings: "VALID_WITH_WARNINGS",
  Validating: "VALIDATING",
  Invalid: "INVALID",
  Missing: "MISSING",
} as const;
/**
 * Lifecycle state of the credentials installed for a datasource instance. Mirrors the internal admin Status enum so the handler can surface the same health signals already tracked today. EXPIRING_SOON is represented as VALID_WITH_WARNINGS (with detail in `message`); EXPIRED is surfaced as INVALID plus a non-null `expiresAt` in the past.
 *
 * @remarks
 */
export type DatasourceCredentialStatus = ClosedEnum<
  typeof DatasourceCredentialStatus
>;

/** @internal */
export const DatasourceCredentialStatus$inboundSchema: z.ZodNativeEnum<
  typeof DatasourceCredentialStatus
> = z.nativeEnum(DatasourceCredentialStatus);

/** @internal */
export const DatasourceCredentialStatus$outboundSchema: z.ZodNativeEnum<
  typeof DatasourceCredentialStatus
> = DatasourceCredentialStatus$inboundSchema;

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace DatasourceCredentialStatus$ {
  /** @deprecated use `DatasourceCredentialStatus$inboundSchema` instead. */
  export const inboundSchema = DatasourceCredentialStatus$inboundSchema;
  /** @deprecated use `DatasourceCredentialStatus$outboundSchema` instead. */
  export const outboundSchema = DatasourceCredentialStatus$outboundSchema;
}
