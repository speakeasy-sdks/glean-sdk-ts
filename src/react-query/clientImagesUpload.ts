/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GleanCore } from "../core.js";
import { clientImagesUpload } from "../funcs/clientImagesUpload.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGleanContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type ClientImagesUploadMutationVariables = {
  request: operations.UploadimageRequest;
  options?: RequestOptions;
};

export type ClientImagesUploadMutationData = components.UploadImageResponse;

/**
 * Upload images
 *
 * @remarks
 * Upload images for various types (profile pic, background, UGC thumnail/content, etc) with additional metadata.
 */
export function useClientImagesUploadMutation(
  options?: MutationHookOptions<
    ClientImagesUploadMutationData,
    Error,
    ClientImagesUploadMutationVariables
  >,
): UseMutationResult<
  ClientImagesUploadMutationData,
  Error,
  ClientImagesUploadMutationVariables
> {
  const client = useGleanContext();
  return useMutation({
    ...buildClientImagesUploadMutation(client, options),
    ...options,
  });
}

export function mutationKeyClientImagesUpload(): MutationKey {
  return ["@gleanwork/api-client", "images", "upload"];
}

export function buildClientImagesUploadMutation(
  client$: GleanCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: ClientImagesUploadMutationVariables,
  ) => Promise<ClientImagesUploadMutationData>;
} {
  return {
    mutationKey: mutationKeyClientImagesUpload(),
    mutationFn: function clientImagesUploadMutationFn({
      request,
      options,
    }): Promise<ClientImagesUploadMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(clientImagesUpload(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}
