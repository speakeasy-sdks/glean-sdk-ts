/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { GleanCore } from "../core.js";
import { datasourcesRotateDatasourceCredentials } from "../funcs/datasourcesRotateDatasourceCredentials.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as components from "../models/components/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useGleanContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type DatasourcesRotateDatasourceCredentialsMutationVariables = {
  rotateDatasourceCredentialsRequest:
    components.RotateDatasourceCredentialsRequest;
  datasourceInstanceId: string;
  options?: RequestOptions;
};

export type DatasourcesRotateDatasourceCredentialsMutationData =
  components.DatasourceCredentialStatusResponse;

/**
 * Rotate datasource instance credentials
 *
 * @remarks
 * Rotates the credentials that a datasource instance uses to connect to its upstream system. Replaces the active credential material with the supplied values and returns the credential status after rotation. Access is limited to callers with the ADMIN scope; the handler enforces this check.
 * Only keys recognized as credential material for the datasource type may be set in `credentials.values` (e.g. `clientSecret`, `apiToken`, `privateKey`, depending on the configured auth method). Unrecognized keys, or keys that correspond to non-credential configuration, cause a 400; other instance configuration must be updated via PATCH /configure/datasources/{datasourceId}/instances/{instanceId}.
 */
export function useDatasourcesRotateDatasourceCredentialsMutation(
  options?: MutationHookOptions<
    DatasourcesRotateDatasourceCredentialsMutationData,
    Error,
    DatasourcesRotateDatasourceCredentialsMutationVariables
  >,
): UseMutationResult<
  DatasourcesRotateDatasourceCredentialsMutationData,
  Error,
  DatasourcesRotateDatasourceCredentialsMutationVariables
> {
  const client = useGleanContext();
  return useMutation({
    ...buildDatasourcesRotateDatasourceCredentialsMutation(client, options),
    ...options,
  });
}

export function mutationKeyDatasourcesRotateDatasourceCredentials(): MutationKey {
  return [
    "@gleanwork/api-client",
    "Datasources",
    "rotateDatasourceCredentials",
  ];
}

export function buildDatasourcesRotateDatasourceCredentialsMutation(
  client$: GleanCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: DatasourcesRotateDatasourceCredentialsMutationVariables,
  ) => Promise<DatasourcesRotateDatasourceCredentialsMutationData>;
} {
  return {
    mutationKey: mutationKeyDatasourcesRotateDatasourceCredentials(),
    mutationFn: function datasourcesRotateDatasourceCredentialsMutationFn({
      rotateDatasourceCredentialsRequest,
      datasourceInstanceId,
      options,
    }): Promise<DatasourcesRotateDatasourceCredentialsMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(datasourcesRotateDatasourceCredentials(
        client$,
        rotateDatasourceCredentialsRequest,
        datasourceInstanceId,
        mergedOptions,
      ));
    },
  };
}
